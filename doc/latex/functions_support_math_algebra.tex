\funcitem \cppinline|vec<2,T> matrix::product(vec<2,T> a, b)| \itt{matrix::product}

\cppinline|vec<1,V> matrix::product(vec<2,T> a, vec<1,U> b)|

\cppinline|vec<1,V> matrix::product(vec<1,T> b, vec<2,U> a)|

\funcitem \cppinline|vec<2,T> matrix::transpose(vec<2,T> a)| \itt{matrix::transpose}

This function is identical to the generic \cppinline{transpose()} function, and is also provided in the \cppinline{maxtrix} namespace for consistency.

\funcitem \cppinline|vec<1,T*> matrix::diagonal(vec<2,T>)| \itt{matrix::diagonal}

\funcitem \cppinline|vec2d matrix::make_identity(uint_t)| \itt{matrix::make_identity}

\funcitem \cppinline|vec2d matrix::make_scale(double sx, sy)| \itt{matrix::make_scale}

\cppinline|vec2d matrix::make_scale(double s)|

\funcitem \cppinline|vec2d matrix::make_translation(double dx, dy)| \itt{matrix::make_translation}

\funcitem \cppinline|vec2d matrix::make_rotation(double a)| \itt{matrix::make_rotation}

\funcitem \cppinline|vec1d matrix::make_point(double x, y)| \itt{matrix::make_point}

\funcitem \requirelib{lapack} \cppinline|bool matrix::invert(vec2d a, vec2d& i)| \itt{matrix::invert}

\requirelib{lapack} \cppinline|bool matrix::inplace_invert(vec2d&)| \itt{matrix::inplace_invert}

\requirelib{lapack} \cppinline|bool matrix::invert_symmetric(vec2d a, vec2d& i)| \itt{matrix::invert_symmetric}

\requirelib{lapack} \cppinline|bool matrix::inplace_invert_symmetric(vec2d&)| \itt{matrix::inplace_invert_symmetric}

\funcitem \requirelib{lapack} \cppinline|bool matrix::solve_symmetric(vec2d a, vec1d b, vec1d& r)| \itt{matrix::solve_symmetric}

\requirelib{lapack} \cppinline|bool matrix::inplace_solve_symmetric(vec2d& a, vec1d& b)| \itt{matrix::inplace_solve_symmetric}

\funcitem \requirelib{lapack} \cppinline|bool matrix::eigen_symmetric(vec2d a, vec1d& va, vec1d& ve)| \itt{matrix::eigen_symmetric}

\requirelib{lapack} \cppinline|bool matrix::inplace_eigen_symmetric(vec2d& a, vec1d& va)| \itt{matrix::inplace_eigen_symmetric}

\funcitem \requirelib{fftw} \cppinline|vec2cd fft(vec2d)| \itt{fft}

\requirelib{fftw} \cppinline|vec2d ifft(vec2cd)| \itt{ifft}

\funcitem \cppinline|vec<1,W> convolve(vec<1,T> x, vec<1,U> y, vec<1,V> k)| \itt{convolve}
